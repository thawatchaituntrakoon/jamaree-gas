# PROMPTS.md — พรอมต์สั้น เห็นผลไว ประหยัดโทเคน

ก๊อปวางบรรทัดในเครื่องหมาย `"..."` ไปสั่ง Claude Code ได้ทันที (แก้คำในวงเล็บ `< >` เป็นของธุรกิจคุณก่อนกด)
ทุกครั้งที่แก้ระบบเสร็จ ให้เปิดดูใน browser หรือรีเฟรชหน้าที่เปิดไว้เพื่อเช็กผลก่อนใช้งานจริง

---

### 1) เปลี่ยนชื่อร้าน + ชื่อระบบทั้งสวีทเป็นธุรกิจของฉัน
```
เปลี่ยนชื่อร้าน/ชื่อระบบในทุกไฟล์ของ bizsuite (index.html, erp.html, และหน้าที่แตะ) จาก "BizSuite — ระบบธุรกิจของฉัน" เป็น "<ชื่อธุรกิจของฉัน>" ให้ดูเป็นแบรนด์เดียวกันทั้งชุด
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 2) เปลี่ยนธีมสีหลัก
```
เปลี่ยนสี accent หลักของ bizsuite จากน้ำเงิน #2563eb เป็น <สีที่ต้องการ เช่น เขียวเข้ม #0d7a4a> ทั้ง erp.html และ skin.css ให้เข้ากันทั้งชุด อย่าแก้โครงสร้าง/เลย์เอาต์
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 3) เพิ่มโมดูลใหม่เข้า erp.html
```
/new-module สร้างโมดูล<ชื่อโมดูล เช่น จัดซื้อ, นัดหมาย, พนักงาน>
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 4) เพิ่มการ์ดสรุปในแดชบอร์ด erp.html
```
เพิ่มการ์ดสรุป "<สิ่งที่อยากเห็น เช่น ยอดขายเดือนนี้ / ของใกล้หมด>" ในแท็บแดชบอร์ดของ erp.html คำนวณสดจากข้อมูลจริงใน db ห้ามเก็บค่าซ้ำลง localStorage
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 5) แก้เมนู/ข้อความ
```
แก้ข้อความ/ป้ายในแท็บ "<ชื่อแท็บ>" ของ erp.html จาก "<ข้อความเดิม>" เป็น "<ข้อความใหม่>" อย่าแก้โครงสร้างหรือฟังก์ชันอื่น
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 6) เพิ่มสินค้า/ข้อมูลตัวอย่างชุดธุรกิจฉัน
```
แก้ข้อมูลตัวอย่างเริ่มต้น (seed) ใน erp.html ให้เป็นสินค้า/ลูกค้าของธุรกิจฉันจริง เช่น <รายการสินค้า/ลูกค้า 3-5 รายการ> แทนของตัวอย่างเดิม
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 7) ลบหน้าโชว์เคสที่ไม่ใช้ออกจาก hub
```
เอาการ์ด "<ชื่อโดเมน เช่น การตลาด>" ออกจากหน้า index.html ของ bizsuite (ไม่ต้องลบไฟล์ .html ทิ้ง แค่เอาลิงก์ออกจากหน้ารวม)
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 8) ตรวจระบบก่อนใช้จริง
```
ตรวจ bizsuite ทั้งชุด: ทุกลิงก์ในหน้าใช้งานได้จริง, erp.html เปิดแล้วบันทึก/แก้/ลบข้อมูลได้ครบทุกแท็บ, ไม่มี error ใน console, ไม่มีข้อมูลธุรกิจจริงหลุดอยู่ในหน้าโชว์เคส
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 9) ออกใบเสนอราคา/ใบแจ้งหนี้ให้ลูกค้า
```
ที่แท็บ "เอกสาร" ของ erp.html ออกใบเสนอราคาให้ลูกค้า "<ชื่อลูกค้า>" รายการ "<ชื่อสินค้า/บริการ, จำนวน, ราคา>" แล้วช่วยอธิบายวิธีกดแปลงเป็นใบแจ้งหนี้กับกดรับชำระให้หน่อย
```

---

### 10) กรอกข้อมูลกิจการสำหรับหัวเอกสาร
```
ช่วยเตือนให้ฉันกรอกที่อยู่ร้าน+เลขผู้เสียภาษี ที่ปุ่ม "🏢 ข้อมูลกิจการ" บน header ของ erp.html ก่อนพิมพ์เอกสารจริงครั้งแรก (ไม่งั้นหัวเอกสารจะว่าง)
```

---

### 11) เพิ่มพนักงาน + ตั้งค่าโควตาลา
```
ที่แท็บ "พนักงาน" ของ erp.html เพิ่มพนักงานใหม่ "<ชื่อ>" ตำแหน่ง "<ตำแหน่ง>" เงินเดือน <จำนวน> บาท แล้วช่วยพาไปตั้งค่าโควตาลาที่แท็บ "ลงเวลา & ลา" ให้ตรงกับนโยบายร้านฉัน (<ลากิจ/ลาป่วย/ลาพักร้อน กี่วัน/ปี>)
```

---

### 12) สร้างงวดเงินเดือนเดือนนี้
```
ที่แท็บ "เงินเดือน" ของ erp.html สร้างงวดเงินเดือนเดือนนี้ ใช้อัตราประกันสังคม <กี่ %> เพดาน <กี่บาท> (ปกส.และภาษีหัก ณ ที่จ่ายคำนวณอัตโนมัติให้แล้ว — แก้มือได้ในตารางก่อนกด "บันทึก") ก่อนกด "จ่ายงวดนี้" ช่วยพาดาวน์โหลด CSV ไฟล์จ่ายเงินเดือนเข้าธนาคารให้ด้วย
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 13) เปิดจด VAT + ออกเอกสารรวม VAT
```
ที่ปุ่ม "🏢 ข้อมูลกิจการ" บน header ของ erp.html ติ๊ก "จด VAT" ให้ฉัน แล้วช่วยพาไปออกใบแจ้งหนี้ให้ลูกค้า "<ชื่อลูกค้า>" แบบติ๊กรวม VAT 7% ด้วย
```

---

### 14) บันทึก OT ให้พนักงาน
```
ที่แท็บ "ลงเวลา & ลา" ของ erp.html บันทึก OT ให้พนักงาน "<ชื่อ>" วันที่ <วันที่> จำนวน <กี่ชม.> ชั่วโมง อัตรา <กี่บาท>/ชม.
```

---

### 15) ดูโปรไฟล์พนักงาน + พิมพ์หนังสือรับรองการทำงาน
```
ที่แท็บ "พนักงาน" ของ erp.html คลิกชื่อ "<ชื่อพนักงาน>" ดูสรุปเดือนนี้ให้หน่อย แล้วช่วยพิมพ์หนังสือรับรองการทำงานให้ (<โชว์/ไม่โชว์เงินเดือน>)
```

---

### 16) เพิ่ม/ลบหมวดเงินเข้า-ออก
```
ที่แท็บ "บัญชี" ของ erp.html ส่วน "หมวดตั้งเอง" เพิ่มหมวด<เงินเข้า/เงินออก> ชื่อ "<ชื่อหมวด>" ให้ฉัน
```

---

### 17) เพิ่มแพคเกจเงินเพิ่ม/เงินหักประจำให้พนักงาน
```
ที่แท็บ "พนักงาน" ของ erp.html คลิกชื่อ "<ชื่อพนักงาน>" → แท็บ "แพคเกจ" เพิ่มรายการ "<ชื่อรายการ เช่น ค่าตำแหน่ง/หักกู้ยืม>" ประเภท <เพิ่ม/หัก> จำนวน <กี่บาท>/งวด ให้ฉัน (รายการนี้จะไหลเข้างวดเงินเดือนทุกงวดอัตโนมัติ)
```

---

### 18) บันทึกพนักงานพ้นสภาพ (ลาออก/เลิกจ้าง)
```
ที่แท็บ "พนักงาน" ของ erp.html คลิกชื่อ "<ชื่อพนักงาน>" → "🚪 บันทึกพ้นสภาพ" เหตุผล <ลาออก/เลิกจ้าง> วันที่ <วันที่> (ถ้าเลิกจ้าง ระบบคำนวณค่าชดเชย ม.118 ให้อัตโนมัติ) แล้วช่วยพิมพ์เอกสารที่เกี่ยวข้องให้
```

---

### 19) ออกใบแจ้งหนี้แบบหัก ณ ที่จ่าย ให้ลูกค้านิติบุคคล
```
ที่แท็บ "เอกสาร" ของ erp.html ตั้งลูกค้า "<ชื่อลูกค้า>" เป็นนิติบุคคล (กรอกเลขผู้เสียภาษีด้วย) แล้วออกใบแจ้งหนี้รายการ "<รายการ, จำนวน, ราคา>" เลือกหัก ณ ที่จ่ายประเภท "<ค่าบริการ/ค่าเช่า/ขนส่ง/โฆษณา/วิชาชีพอิสระ>" ให้หน่อย พร้อมอธิบายยอดโอนจริงที่จะได้รับ
```

---

### 20) บันทึกรายจ่ายที่หักภาษี + พิมพ์ใบ 50 ทวิ
```
ที่แท็บ "การเงิน" ของ erp.html บันทึกรายจ่าย "<รายการ>" จำนวน <กี่บาท> หัก ณ ที่จ่าย <กี่ %> ผู้รับเงิน "<ชื่อ+เลขผู้เสียภาษี>" แล้วช่วยพิมพ์ใบ 50 ทวิ ให้หน่อย
```

---

### 21) ตั้งค่าพร้อมเพย์ให้ขึ้น QR บนใบแจ้งหนี้
```
ที่ปุ่ม "🏢 ข้อมูลกิจการ" บน header ของ erp.html กรอกเบอร์พร้อมเพย์/เลขผู้เสียภาษีพร้อมเพย์ให้ฉัน แล้วช่วยเช็คว่าใบแจ้งหนี้ที่พิมพ์ออกมาโชว์ช่องทางชำระ PromptPay ถูกต้อง
```

---

### 22) ตั้งกะการทำงาน + เปิดโหมด Kiosk เช็คอิน
```
ที่แท็บ "ลงเวลา & ลา" ของ erp.html ตั้งกะ "<ชื่อกะ>" เวลา <เข้างาน>-<เลิกงาน> แล้วกำหนดให้พนักงาน "<ชื่อ>" ใช้กะนี้ จากนั้นเปิด "🖥️ เปิดจอเช็คอิน" ให้ดูหน่อยว่าใช้งานยังไง
```

---

### 23) ปิดเดือนบัญชี
```
ที่แท็บ "บัญชี" ของ erp.html ส่วน "ปิดเดือน" ช่วยพาปิดเดือน <เดือน-ปี> ให้หน่อย พร้อมเช็ค checklist ให้ครบก่อนปิด
```

---

### 24) สร้างระบบพนักงาน+ลงเวลา+ลา
```
/hr-module สร้างระบบพนักงาน+ลงเวลา+ลาให้ร้านผม
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 25) ออกใบเสนอราคา-ใบเสร็จจากแอป
```
/sales-module อยากออกใบเสนอราคา-ใบเสร็จจากแอป
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 26) ตรวจระบบก่อนใช้จริง
```
/edgecase ตรวจระบบก่อนใช้จริง
```
เสร็จแล้วสั่ง `/golive` เพื่ออัปเดตเว็บ

---

### 27) ดูรายละเอียดลูกค้า/สินค้า/ออเดอร์/เอกสาร
```
คลิกชื่อลูกค้า/สินค้า/เลขเอกสาร/ออเดอร์ ที่ไหนก็ได้ในแอป เพื่อเปิดหน้ารายละเอียดของมัน — จากหน้าลูกค้ากด "สร้างออเดอร์" ตั้งต้นให้เลย
```

---

### 28) มอบหมายออเดอร์ให้พนักงานคนไหนดูแล
```
ไปหน้าออเดอร์ (คลิกปุ่ม "ดู" ในตารางออเดอร์) แล้วเลือกช่อง "ผู้รับผิดชอบ" — จะไปโผล่ในโปรไฟล์พนักงานคนนั้น (แท็บพนักงาน) และการ์ด "งานค้างรายคน" ในแดชบอร์ด
```

---

### 29) กรอกที่อยู่กิจการ/สาขา + ที่อยู่ลูกค้า (จำเป็นต่อใบกำกับภาษีเต็มรูป)
```
กรอก "ที่อยู่" และ "สาขา" ในเมนู 🏢 ข้อมูลกิจการ + เติม "ที่อยู่"/"เลขผู้เสียภาษี" ให้ลูกค้าแต่ละคนที่หน้าแก้ลูกค้า — ถ้าจด VAT แล้วข้อมูลไม่ครบ ระบบจะกันไม่ให้ออกใบกำกับภาษีจนกว่าจะเติมครบ
```

---

### 30) พิมพ์ใบเสร็จรับเงิน/ใบกำกับภาษีเต็มรูป
```
ไปแท็บเอกสารการค้า กดปุ่ม "พิมพ์" ที่ใบเสร็จ — ได้ต้นฉบับ+สำเนาในครั้งเดียว มีเลขผู้เสียภาษีทั้ง 2 ฝั่ง + ยอดเป็นตัวอักษรไทย (ถ้าไม่จด VAT จะออกเป็นใบเสร็จรับเงินธรรมดา)
```

---

### 31) เริ่มใช้จริง — ล้างข้อมูลตัวอย่างแล้วกรอกข้อมูลกิจการ
```
กดปุ่ม "🚀 เริ่มใช้จริง" ท้าย sidebar — ระบบจะสำรองไฟล์ข้อมูลเดโม่ให้อัตโนมัติก่อนลบเสมอ แล้วพาไปกรอกข้อมูลกิจการทีละขั้น (ชื่อ/ที่อยู่/เลขภาษี/สาขา/VAT/พร้อมเพย์/เวลาเข้างาน) จบแล้วลงแดชบอร์ดว่าง ๆ พร้อมเริ่มกรอกสินค้า/ลูกค้าตัวจริง
```

---

### 32) ค้นหากลาง — หาลูกค้า/สินค้า/พนักงาน/เอกสาร/PO จากที่เดียว
```
พิมพ์ในช่องค้นหาใต้ชื่อร้าน (sidebar) อย่างน้อย 2 ตัวอักษร — เจอทั้งลูกค้า สินค้า พนักงาน เลขเอกสาร ออเดอร์ ผู้ขาย ใบสั่งซื้อ คลิกหรือกด Enter ที่ผลลัพธ์แรกเพื่อเปิดหน้ารายละเอียด กด Esc เพื่อปิด
```

---

### 33) เตือนสำรองข้อมูล
```
ถ้าไม่ได้กดสำรองข้อมูลเกิน 7 วัน (หรือไม่เคยสำรองเลย) และมีข้อมูลจริงในระบบแล้ว จะมีแถบเหลืองโผล่เหนือเนื้อหาทุกแท็บเตือนให้กด "สำรองเดี๋ยวนี้" — กดแล้วแถบหายทันที
```

---

### 34) มูลค่าสต๊อก + ตรวจนับสต๊อก
```
ดูมูลค่าสต๊อกรวม (คงเหลือ×ต้นทุน) ที่การ์ดบนแดชบอร์ดหรือแท็บบัญชี — สินค้าที่ยังไม่กรอกต้นทุนจะไม่ถูกนับและมีหมายเหตุบอกจำนวน
กดปุ่ม "📋 ตรวจนับสต๊อก" ในแท็บสินค้า กรอกจำนวนที่นับได้จริง (เว้นว่าง=ไม่เปลี่ยน) ระบบจะสรุปผลต่างให้ดูก่อน กดยืนยันแล้วค่อยปรับสต๊อกเป็นรายการ "ปรับเพิ่ม/ปรับลด" ในประวัติการเคลื่อนไหว
```

---

### 35) จัดซื้อ — สร้าง PO → ส่งสั่งซื้อ → รับของเข้าสต๊อก → จ่ายเจ้าหนี้
```
ไปเมนู "🚚 จัดซื้อ" (อยู่ในหมวดคลังสินค้า) กด "+ สร้าง PO" เลือกผู้ขาย+รายการสินค้า → กด "ส่งสั่งซื้อ" → พอของมากด "รับของเข้าสต๊อก" (แก้จำนวนรับจริงต่อบรรทัดได้ถ้าของมาไม่ครบ) ระบบจะเพิ่มสต๊อกตามจริง + ตั้งบิลเจ้าหนี้อัตโนมัติให้ทันที (โผล่ในแท็บบัญชี → เจ้าหนี้) พอจ่ายเงินจริงกดปุ่ม "จ่ายแล้ว" ที่บิลนั้น
```

---

### 36) สร้าง PO จากของใกล้หมด
```
ที่การ์ด "⚠️ ของใกล้หมด/หมด" บนแดชบอร์ด หรือปุ่มบนแท็บสินค้า กด "สร้าง PO" — ระบบจะ prefill รายการสินค้าที่ต่ำกว่าจุดสั่งซื้อ (lowAt) พร้อมจำนวนแนะนำให้ แก้ผู้ขาย/จำนวน/ราคาต่อได้ก่อนบันทึกจริง
```
